{
  "name": "scoreboard-pro-home",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "dev:all": "node scripts/dev.mjs",
    "dev:operator": "cd apps/operator && npx vite --port 5173",
    "dev:display": "cd apps/display && npx vite --port 5174",
    "build": "vite build",
    "build:all": "npm run build && cd apps/operator && npx vite build && cd ../display && npx vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "@vitejs/plugin-react": "^4.3.0",
    "concurrently": "^9.2.1"
  }
}